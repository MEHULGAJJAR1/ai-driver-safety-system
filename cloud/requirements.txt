# Cloud sync & analytics service (FastAPI + MongoDB Atlas)
# Pinned, conservative versions. Install:  pip install -r cloud/requirements.txt
fastapi==0.111.0
uvicorn[standard]==0.30.1
motor==3.4.0                # async MongoDB driver (Atlas)
pydantic==2.7.1
python-dotenv==1.0.1        # load .env in development
# NOTE: the shared analytics modules (../analytics, ../config.py) are reused
# directly - no extra dependency. The Dockerfile copies them into the image.
